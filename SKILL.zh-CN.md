---
name: eval-driven-iteration
description: "Use for AI, Agent, RAG, Text2SQL, retrieval, or classification systems when evaluation metrics are poor or changing, or when someone proposes tuning prompts, models, or retrieval parameters. Diagnose the failing stage and formulate a falsifiable hypothesis before changing the system. Make the smallest general fix, then validate in this order: known failures, known successes, independent samples when available, and only then the full evaluation. Never present an untested idea as a verified improvement. Trigger this skill whenever someone asks why accuracy changed, whether to tune a prompt, or which version is better, even if they do not use the words evaluation or diagnosis."
---

# Eval-Driven Iteration

This document is the normative instruction for agents. The human-facing explanations are in `README.md` and `README.zh-CN.md`.

这不是一套"帮你自动改代码变强"的技巧,而是一套用来防止以下几种常见翻车模式的纪律:

- **改错层**:检索的问题被当成生成的问题去改（或反过来）,数字可能碰巧变了,但真正的病因没被碰到。
- **对个案打补丁**:规则被调整到刚好能让手头这几个失败案例通过,却没检查有没有顺手弄坏了原本正常的案例,也没检查这个规则对没见过的新案例是否依然成立。
- **对比样本不对等**:两个版本各自的失败样本不一样（比如一个因为环境故障丢了几条、另一个没丢）,却直接拿汇总数字做对比,得出的"谁更好"其实只是分母不同造成的假象。
- **无证据的结论**:一个改动"听起来应该有用",没有真的跑一遍验证,就被写成"已生效""已提升"。
- **一次改三样**:为了省时间,把好几个可能的修复捆在一起改完再重跑,结果数字变了也说不清是哪个改动起的作用，甚至可能一个改动在帮忙、另一个在拖后腿，两者相互抵消。

## 核心流程

```
建立基线 → 跑评测 → 错误分析（先分类,再猜原因）→ 提出可证伪假设 → 最小修复 → 分层回归验证 → 全量评测
```

每一步都要走到,不要因为"原因看起来很明显"就跳过分类这一步——恰恰是"看起来很明显"的原因，最容易是分错层、对个案打了补丁、或者被样本不对等误导出来的。

### 1. 建立基线

动手改任何东西之前,先确认手上这个基线数字本身可信。

- 基线和后续任何对比,必须用同一批 case、同一套环境、同一个随机种子（如果适用）。做不到这一点,要在报告里明确说清楚,不能装作可比。
- 如果基线本身长得可疑（接近 0%、接近 100%，或者和预期差距巨大）,先别急着相信它,去验证评测链路本身有没有问题（见后面"先信链路,再信数字"）。
- 记录清楚这个基线是在什么配置下跑出来的：模型/版本、参数、Prompt 版本、代码或数据快照，保证以后能复现。

### 2. 跑评测

跑出一个总分,但更重要的是拿到**逐 case 的结果**，不是只有一个汇总数字。只有汇总数字,没法做错误分析——那只是一个事实,不是诊断。

报告数字时要诚实：
- 任何百分比后面都要带上真实分母（比如写"12/48"而不是只写"25%"）。不带分母的百分比很容易被误读。
- 如果有 case 因为评测链路本身的问题（超时、接口故障、脚本报错）被剔除,单独统计,不要和"系统本身答错了"混在一起算进失败率。
- 小样本上的波动不要轻易当成趋势。10 条里差 1 条，不构成结论。

#### 网络/模型随机性下的幂等性边界

网络抖动、服务端排队和 LLM 采样会使同一评测重复运行时的耗时、token
用量和重试次数自然波动。因此，不能把所有运行字段都要求 byte 级一致。
在这类环境下，幂等性验收至少要求：

- 输入快照、case 集合、模型/Prompt/检索配置一致；
- 每个 case 的最终归因标签（成功或具体失败类型）一致；
- 成功 case 的标准化结果集一致（包含行内容及多重集合语义）；
- Recall/Precision 等能力指标在同一分母口径下可复算。

以下字段允许在报告中单独记录并出现波动，不得据此判定评测结果不一致：

- 单 case 及全量运行耗时（包括 P50/P95）；
- token 消耗（尤其是服务端未返回 usage 时）；
- 重试次数、退避等待时间和各次错误信息；
- 网络连接建立时间及模型服务端排队时间。

若最终标签或结果集发生变化，必须逐 case 诊断是模型随机性、基础设施失败、
还是评测逻辑缺陷；不能用“耗时不同”掩盖真正的结果不一致。

### 3. 错误分析——先分类,再猜原因

这是最容易被跳过、也是浪费迭代次数的头号原因。提出任何修复方案之前,先把每一条失败 case 归到具体的失败阶段里。不要对着一堆失败 case 直接猜一个笼统的原因,规律应该是从分类里长出来的,不是先猜出来再往上套。

**默认失败分类表**（可以按具体领域改名字，但阶段一定要分开,不能合并）：

| 分类 | 含义 | 典型症状 |
|---|---|---|
| 检索/召回失败 | 系统压根没找到需要的信息/候选 | 需要的字段/文档从没进过候选池 |
| 结构/关联失败 | 该找的东西都找到了,但彼此之间的关联关系缺失或错误 | Join 键没被识别成属于某张相关表；实体都找到了但关系没建立 |
| 排序/筛选失败 | 候选都在池子里,但排序或阈值把正确的挤了出去 | 分数不够高被阈值淘汰、或被 Top-K 截断挡在门外 |
| 生成/推理失败 | 该有的信息都给到了,结果还是错的 | 字段都在，但生成逻辑本身错了（漏了去重、多加了限制条件、加错了过滤条件） |
| 校验/安全失败 | 输出被规则挡下来了，无论挡得对不对 | 合理的查询被过严的安全规则拦截 |
| 执行/链路故障 | 评测或生产链路本身崩了,不是系统能力问题 | 接口超时、脚本报错、依赖不可用 |
| 最终呈现失败 | 底层结果是对的，但最后展示/组织成的答案错了 | 数据算对了，最后总结成的那句话说错了字段 |

每一条失败 case，都要写清楚：归到哪一类、支撑这个判断的具体证据是什么（不是猜测）。"大概是检索的问题"不算证据；"trace 里确认这个字段从没出现在传给生成模块的候选列表里"才算证据。

**不要让一个看起来很戏剧化的笼统原因，把整批失败 case 都吞掉。** 一批失败集中显示"链路故障"或"安全拦截"，往往是评测脚本本身有 bug 把结果误判了，而不是系统真的这么差——见下面"先信链路，再信数字"。

### 4. 提出可证伪假设

针对每一个 case 数量足够多的失败分类,写一句能被证伪的假设：*"如果做了 X，属于 Y 类的失败 case 会从失败变成成功，不属于 Y 类的 case 不受影响。"*

如果假设写不出这么具体的形式,说明错误分析还没做到位,回到上一步继续拆。

明确把跨层的假设分开处理。永远不要把"顺便把 Prompt 也改一改、阈值也调一调、Schema 也顺手改一改"打包成一次性的改动去跑——这是在制造归因混乱，等数字变了，会完全说不清是哪个改动起了作用，甚至可能其中一个改动在帮忙、另一个在拖后腿，两者相互抵消看不出来。

### 5. 最小修复

只实现能验证这条假设所需的最小改动。几条经验：

- **一个根因、一个修复、一次独立验证**——不要在验证任何一个改动是否有效之前,先把好几个不相关的修复捆在一起。
- **优先写成通用规则，而不是针对个案的补丁。** "除非满足 Y，否则不要做 X" 这种写法可以复用；"如果 case_id 是这个，就特殊处理" 不是修复，是记住了一个答案。如果一条修复脱离了它最初依据的那个具体 case 就没法表述清楚,说明这条修复还没写完。
- **提前说清楚这次修复不应该影响到什么。** 每个修复都有影响范围，动手之前就该点名"哪些 case/行为应该保持不变"——这样后面的回归验证才有意义,不然回归验证就只是走个形式。
- 如果修复是调一个参数（阈值、Top-K、上限）,不要凭感觉猜新值——如果已经有分数/日志在手,先离线算一遍敏感性曲线，不需要为了试五个阈值就重新跑一遍完整的模型调用。

### 6. 分层回归验证——顺序不能乱，不能合并成一次跑

回归验证有两个完全不同的目的，必须分开检查，不能揉进一次汇总重跑里：

**6a. 已知失败 case 回归。** 重新跑这次修复针对的具体 case，确认它们现在通过了，而且要确认"通过的原因"和假设一致——不是只看结果对不对。（如果一条 case 现在通过了，但 trace 显示它是因为别的、和这次修复无关的原因才通过的，这不算验证成功，要单独标注出来。）

**6b. 已知成功 case 回归（这一步不能省）。** 抽一批修复之前本来就通过的 case——最好是和这次要修的失败分类完全无关的——重新跑一遍，确认它们依然通过。这一步专门用来抓"修好了 3 条、悄悄弄坏了 5 条"这种情况。一个修复如果只拿它针对的那几条 case 去验证，就从来没有真正被测试过"会不会在别的地方帮倒忙"这个可能性。

**6c. 独立样本检查（如果条件允许）。** 如果手上有一批从没被用来诊断这个问题、也没被用来设计这次修复的 case，抽几条加进回归里。参与过错误分析的 case，不能算是检验"这个修复是否真的通用"的干净样本——它们更像是训练集。只拿这些 case 证明修复有效,是一种变相的过拟合。

6a、6b（以及条件允许时的 6c）全部通过，这次修复才有资格进入下一步。

### 7. 全量评测

只有在小范围、有针对性的回归全部通过之后，才进行一次完整的全量重新评测。这一步应该是整个流程里成本最高、门槛最严的一步——它应该是最后才做的事，不是第一反应就想做的事。

报告全量重跑结果时：
- 对比基线时用**同一批 case**（见下面"分母要对等"）——不要拿新一轮的 N 条去和旧一轮不同的 M 条比出一个差值。
- 按失败分类报告变化，不要只报一个总分。"执行准确率从 40% 到 45%" 远不如 "结构/关联类失败从 12 条降到 3 条，生成类失败维持在 5 条不变，没有出现新的失败类型" 有信息量。
- 如果数字变化没达到预期、甚至方向反了，回到第 3 步对新的失败集合重新做错误分析——不要在同一个修复上继续盲目调参。

## 不能妥协的几条规则

这几条值得明确写出来，因为它们是最容易在赶时间时被跳过的：

1. **没有先做失败分类诊断，就不能提修复方案。** 看到数字低不算诊断。
2. **没有跑出来的证据，不能说"这样改有效"。** "应该有效"是假设，不是结论。永远不要把一个没验证过的修复，报告成已经生效的结果。
3. **一次改动、一个可归因的原因。** 如果一次性改了好几处，就没法说清楚结果是哪个改动带来的了。如果时间紧张必须捆在一起改，要在报告里明确说明这一点，并标注归因是不确定的。
4. **已知成功 case 的回归是必须项，不是可选项。** 省掉这一步，就是"修好的功能"悄悄弄坏别的东西却没人发现的根源。
5. **分母要对等。** 任何两个要拿来对比的数字（改动前后、方案 A vs 方案 B），必须来自同一批 case。做不到，先说明白，最好重新在两边的交集上算一遍再比。
6. **小样本要谨慎。** N 很小的时候，差 1-2 条 case 通常是噪声，不是信号。任何百分比旁边都要带上 N。不要拿个位数样本量的数字去撑一个大结论，除非同时说明这一点。
7. **先信评测链路本身，再信数字。** 一批失败如果呈现出可疑的、过于整齐划一的模式（成功率恰好是 0%、全都归在同一类、全都卡在同一个阶段），这种情况和"系统真的这么差"相比，同样可能是评测链路本身出了 bug。诊断系统之前，先用一个"理应必然通过"的阳性对照 case 验证评测链路本身没问题，如果相关的话，再加一个"故意构造成应该失败"的阴性对照，确认检查逻辑真的会拦截错误。
8. **说清楚这次没覆盖到什么。** 每次修复都有它没管到的 case 和没解决的失败模式。要说明哪些还悬而未决，不要因为眼前这批 case 都通过了，就暗示已经全面覆盖。

## 输出模板

每一轮"诊断+修复"，用这个结构记录，不管是要汇报给人看，还是自己留档备查：

```markdown
## [这次修复的简短名称]

**现象：** [看起来不对的指标/行为，带上真实的 N 和分母]

**证据：** [从 trace/日志里实际观察到的东西——不是推断，是确实看到的原始事实]

**失败分类：** [归入分类表里的哪一类，或领域内对应的等价分类——说明为什么归到这一类，不能只给个标签]

**根因假设（可证伪）：** [如果做了 X，属于 Y 类的 case 会从失败变成成功，不属于 Y 类的不受影响]

**修复方案（最小、写成通用规则）：** [具体改动，表述成一条规则，不是针对某条具体 case 的补丁]

**明确排除在外/不受此次修复影响的范围：** [这次修复没有、也不打算解决的其他失败类型或边界情况]

**已知失败 case 回归：** [修复的 case 是否通过，以及通过的机制是否和假设一致——不只是看结果]

**已知成功 case 回归：** [抽样的原本成功 case，确认没有引入退化]

**独立样本检查（如有）：** [诊断集合之外的 case，确认这个修复是否真的通用]

**结论——是否可以进入全量评测：** [是/否，以及为什么]
```

## 输出物建议

- 每一轮诊断，独立产出一份诊断报告文件，不要把多轮诊断混写在同一个文件里滚动更新，方便追溯"当时基于什么证据做的判断"
- 涉及数据/代码改动时，保留旧版本产出（不要覆盖），新开一个可区分的实验名/文件名，允许新旧结果并列对比
- 最终汇总报告里，清楚区分"已验证生效的修复"和"已诊断但未验证的方向"——后者可以作为后续工作的待办事项列出，但不能和前者混在一起当作已完成的成果陈述

## 用在检索/查询系统之外的场景

上面的分类表和流程本身是领域无关的，几个具体映射帮助理解怎么套用到别的系统上：

- **RAG 系统**：检索/召回失败 = 检索到的片段不对或缺失；结构/关联失败 = 片段都检索到了，但串联这些片段的推理链断了；生成失败 = 片段都在上下文里，模型依然理解错或幻觉。
- **分类/传统机器学习模型**：没有直接对应"检索"这一类，可以理解成"特征/输入失败"——做出正确判断所需的信号，压根没在输入里出现过；生成失败对应"信号在，但模型权重判断错了"，经典的校准、阈值调优问题大多落在这一类。
- **Agent / 工具调用系统**：结构/关联失败 = Agent 选对了工具，但参数传错了或调用顺序不对；校验/安全失败 = 一次合理的工具调用被过于保守的护栏拦下来了；执行/链路故障 = 工具超时或接口报错，和 Agent 本身的推理无关。
- **推荐系统**：排序/筛选失败往往是最主要的一类——正确的物品其实在候选集里，只是没被排到前面展示出来；这一类最容易被误诊为"模型能力不够"而去重新训练整个模型，其实只是重排序或阈值的问题。

不管套用在哪个领域，纪律是一样的：先分类再猜原因，只改能验证一个假设的最小范围，同时对着已知失败和已知成功两边验证过再下结论，永远不要把一个没跑过的猜测，包装成已经验证过的结果去汇报。
